FROM python

COPY ["TEST.sql", "./"]

RUN sudo mysqladmin create mybd &&\
mysql -u root -p mybd < TEST.sql &&\
sudo mysql -u root -p &&\
CREATE USER 'user'@'localhost' IDENTIFIED BY '1234'; &&\
GRANT ALL PRIVILEGES ON mybd.* TO 'user'@'localhost'; &&\
FLUSH PRIVILEGES;

WORKDIR /WHN-Front-Back
